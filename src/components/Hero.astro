<section class="hero hero-animate" id="hero">
  <p class="subtitle">Web Developer</p>

  <h1>SHIBA</h1>

  <p class="description">
    フロントエンドを中心に学習中の学生です。<br />
    Astro / HTML / CSS / JavaScript を使って制作しています。
  </p>

  <div class="buttons">
    <a href="#works">Worksを見る</a>
    <a href="#contact">Contact</a>
  </div>
</section>
<style>
.hero {
  min-height: 80vh;
  display: flex;
  flex-direction: column;
  justify-content: center;
  gap: 12px;
}

.subtitle {
  font-size: 16px;
  color: #666;
  letter-spacing: 1px;
}

.hero h1 {
  font-size: 5rem;
  font-weight: 700;
  margin: 0;
}

.description {
  font-size: 16px;
  color: #444;
  max-width: 600px;
}

.buttons {
  margin-top: 20px;
  display: flex;
  gap: 12px;
}

.buttons a {
  display: inline-block;
  padding: 10px 16px;
  border-radius: 8px;
  text-decoration: none;

  font-size: 14px;
  transition: 0.2s;
}

/* メインボタン */
.buttons a:first-child {
  background: #111;
  color: #fff;
}

/* サブボタン */
.buttons a:last-child {
  border: 1px solid #ddd;
  color: #111;
}

.buttons a:hover {
  transform: translateY(-2px);
  opacity: 0.85;
}
</style>