<section class="hero hero-animate">
  <p class="subtitle">Portfolio</p>
  <h1>SHIBA</h1>
  <p class="description">
    BlenderやUnityを使いながら、3D表現とインタラクティブな作品を作っています。
  </p>

  <div class="buttons">
    <a href="/works/">Works</a>
    <a href="/contact/">Contact</a>
  </div>
</section>

<style>
.hero {
  min-height: calc(100vh - 70px);
  display: flex;
  flex-direction: column;
  justify-content: center;
  gap: 14px;
}

.subtitle {
  color: #555;
  font-size: 15px;
  letter-spacing: 0;
}

.description {
  max-width: 620px;
  font-size: 18px;
}

.buttons {
  display: flex;
  flex-wrap: wrap;
  gap: 12px;
  margin-top: 12px;
}

.buttons a {
  display: inline-block;
  padding: 10px 18px;
  border-radius: 8px;
  background: #111;
  color: white;
  text-decoration: none;
}

.buttons a + a {
  background: rgba(255, 255, 255, 0.8);
  color: #111;
  border: 1px solid rgba(0, 0, 0, 0.12);
}

@media (max-width: 900px) {
  .hero {
    min-height: auto;
    padding-top: 16px;
    padding-bottom: 24px;
    gap: 10px;
  }

  .hero h1 {
    font-size: 2.6rem;
  }

  .description {
    font-size: 15px;
    line-height: 1.7;
  }

  .buttons {
    margin-top: 12px;
  }
}
</style>
